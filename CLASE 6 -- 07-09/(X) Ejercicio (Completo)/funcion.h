/** \brief Funcion que hace mayusculas las primeras letras de los nombres o strings ingresados.
 *
 * \param caracter[] char string que va a ser modificado.
 * \return void
 */
void UpperPrimerCaracter(char[]);


/** \brief Funcion que calcula la longitud de una cadena de caracteres.
 *
 * \param char[] string el cual va a ser analizado.
 * \return int retorna la longitud de la cadena.
 *
 */
int LongitudCadena (char[]);


/** \brief Funcion que carga un vector
 *
 * \param char[] vector el cual será cargado
 * \param int[] tam del vector cual será cargado
 * \param char[] mensaje el cual se le mostrara al usuario
 * \return void
 *
 */
void cargarDato (char[], int, char[]);
